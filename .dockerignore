*.o
*.a
.cache/
# Do not ignore .git directory, otherwise the reported build number will always be 0
.github/
.gitignore
.vs/
.vscode/
.DS_Store

build*/

tools/ui/node_modules/

models/*

/llama-cli
/llama-quantize

arm_neon.h
compile_commands.json
Dockerfile

# infcore: не тащим в build-контекст то, что не нужно C++-сборке (движок+infcore).
# Всё ниже отключено флагами (EXAMPLES/TESTS=OFF) или не участвует в сборке.
docs/
benches/
examples/
pocs/
tests/
.claude/
