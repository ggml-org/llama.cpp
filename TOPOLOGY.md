# Topology — environment state for the turbo-sycl-opt validation loop

Generated by RALPH task P0.0 on **2026-07-07** by running live commands.
**Authoritative.** Anything in `RALPH_PROMPT.md`, `RALPH_TASKS.md`, or prior
session transcripts that disagrees with this file is stale.

## Loop home

| Path | Branch | HEAD | Remote |
|---|---|---|---|
| `/mnt/mrgr/llama-cpp-sycl-turbo/Raudbjorn-fork` | `turbo-sycl-opt` | `58a8c5baa7921a39f7cf09238ff312c4c029e37c` ("test : harden turbo correctness harness + scale_inv probe") | `origin` → `https://github.com/Raudbjorn/ggml-llama.cpp` (fetch+push, fork-only — **never push to upstream**; see `AGENTS.md`) |

`turbo-sycl-opt` is 1 commit behind `origin/turbo-sycl-opt`. Working tree clean.
Local-only branch lineage (don't merge into master):

- `master` (track `origin/master`) — clean ggml-org/llama.cpp catch-up.
- `A770-sycl-fix` — addresses PR #7 review threads.
- `xmx-fa` — 2 commits ahead of `origin/xmx-fa`, XMX gate-failed (IGC ICE).
- `turbo-sycl-opt` (THIS branch) — turbo capacity-validation track.

## Sibling checkouts (reference, NOT the loop home)

| Path | Repo? | Branch | HEAD | Role |
|---|---|---|---|---|
| `Raudbjorn-ggml-llama.cpp` | git | `turbo-sycl-fa-vec` | `0181a0cd27fc74bf5ac6c3713c4ebbb2a8c36523` ("test(sycl): mirror InnerQ scale_inv and WHT round-trip in turbo-FA probe") | **Same git repo as `pr7-fix` (worktree)** — see worktree table below. Holds the source-verified FA-correctness fix (KQ-dot OOB) per `RALPH_TASKS` P1.5. Prompt's claim of HEAD `4e56a9980` is stale. |
| `pr7-fix` | git (worktree of `Raudbjorn-ggml-llama.cpp`) | `A770-sycl-fix-2` | `b4b2bf571` ("test(sycl): pass InnerQ scale_inv to WHT in turbo-FA + non-FA probes") | Auxiliary worktree for PR #7 follow-ups. |
| `FellypeMelo-llama-cpp-turboquant-SYCL` | git | `feature/turboquant-kv-cache` | `7eb9efe` ("feat(sycl): implement Turbo2.0/3.0 copy kernels and improve device discovery stability") | Reference: upstream FellypeMelo fork. |
| `TheTom-llama-cpp-turboquant` | git | `feature/turboquant-kv-cache` | `558c6b7` ("docs: add Linux prebuild links") | Reference: upstream TheTom fork. |
| `compare` | not git | — | — | One-off comparison output, not under source control. |
| `PRs` | not git | — | — | PR text dumps / cached review threads. |
| `docs` | not git | — | — | **Shared research doc repo** (`docs/research/*.md`). Updated by all sessions. |

### `Raudbjorn-ggml-llama.cpp` worktrees (single git repo, multiple checkouts)

Listed by `git -C Raudbjorn-ggml-llama.cpp branch -vv`:

- `A770-sycl-fix-2` → `/mnt/mrgr/llama-cpp-sycl-turbo/pr7-fix` (`b4b2bf571`)
- `rebase/turbo-sycl-fa-vec-on-master` → `/tmp/pr5-rebase` (`77fe508a2`) — experimental rebase worktree
- `turbo-sycl-fa-vec` → `Raudbjorn-ggml-llama.cpp/` working tree (`0181a0cd2`) — **canonical FA-correctness work**
- `master` → `904738505` ("sycl : remove broken turbo KV flash-attention; hard-error turbo on Arc") — *this is the same content as `turbo-fix` (a branch that points at `master`'s SHA)*
- `turbo-fix` → same SHA as `master` (alias)

So the **canonical FA-correctness work** is on `Raudbjorn-ggml-llama.cpp` at HEAD `0181a0cd2`. P1.5 must reconcile this against `Raudbjorn-fork` (`turbo-sycl-opt`) before any FA correctness edit lands.

## Listening ports (8080–8099)

Live `ss -tlnp` snapshot:

| Port | Process | Verdict |
|---|---|---|
| `:8080` | SABnzbd (Glitter V2) | unrelated, media-stack |
| `:8082` | qBittorrent WebUI | unrelated, media-stack |
| `:8088` | `/usr/bin/llama-server` PID **4481** (root, started 08:09) — `--model /mnt/ssd1/models/mistral-7b-instruct-v0.1.Q4_K_M.gguf --ctx-size 8192 --batch-size 512 --ubatch-size 256 --threads 12 --cpu-mask 0xffffff --n-gpu-layers 0 --flash-attn auto --cache-type-k q8_0 --cache-type-v q8_0 --host 127.0.0.1 --port 8088` | **SQUATS the port** the SYCL service wants. Not GPU; CPU-only. Health endpoint returns ok. |
| `:8090` | s8n homepage | unrelated |

No port in 8081, 8083–8087, 8089, 8091–8099 is bound.

### llama-sycl.cpp.service state

```
$ systemctl status llama-sycl.cpp.service
○ llama-sycl.cpp.service - llama.cpp Server (SYCL fork build-turbofix / turboquant on Arc A770)
     Loaded: loaded (/etc/systemd/system/llama-sycl.cpp.service; disabled; preset: disabled)
     Active: inactive (dead)
```

**`inactive (dead)`, not crash-looping.** The prompt's "crash-looping ~8h at last check" is from a stale transcript — current state is clean. Port 8088 is held by a manually-launched CPU llama-server (not by this unit). For the validation runs in P1 we don't actually need the service running — `llama-perplexity` and the `test-sycl-turbo-correctness` harness are invoked directly. If a future task needs the live service, the squat must be cleared first (P0 sub-task).

## Toolchain & build artifacts

| Path | Purpose | Notes |
|---|---|---|
| `/home/svnbjrn/build-turbofix/bin/llama-server` (736352 bytes, 2026-07-02) | SYCL fork binary referenced by `llama-sycl.cpp.service` ExecStart | build-id `904738505`, `turbo-fix` branch, IntelLLVM 2026.0 |
| `/usr/bin/llama-server` (14320 bytes, 2026-06-27) | Stock CPU llama-server package | currently squatting :8088 |
| `/mnt/mrgr/llama-cpp-sycl-turbo/Raudbjorn-fork/build-port/` | Active build dir for `Raudbjorn-fork` (`turbo-sycl-opt`) | `bin/test-sycl-turbo-correctness` (700680 B, 2026-07-03), `bin/llama-perplexity`, `bin/llama-server` (SYCL fork), full shared lib set |
| `/home/svnbjrn/build-pr5/` | Older PR #5 build | auxiliary |
| `/home/svnbjrn/build-llama-sycl/` | Reference builds (`do-build.sh`, `do-build-f16-jit.sh`) | auxiliary |
| `/opt/intel/oneapi/setvars.sh` | oneAPI 2026.0 env | needed for SYCL build/run |
| `/opt/sycl/` | open-source DPCPP toolchain (alternative) | NOT the production toolchain |

## Validation models (the 3-model fleet)

| # | Path | Size | Notes |
|---|---|---|---|
| 1 | `/mnt/mrgr/models/mistral-7b-instruct-v0.1.Q4_K_M.gguf` | — | dense, llama-arch, GQA |
| 2 | `/mnt/mrgr/models/llama31-8b-heretic/Meta-Llama-3.1-8B-Instruct-heretic.Q4_K_M.gguf` | — | dense, llama-arch, GQA, long context |
| 3 | `/mnt/mrgr/gguf/Qwen3-Coder-30B-A3B-UD-Q3_K_XL/Qwen3-Coder-30B-A3B-Instruct-UD-Q3_K_XL.gguf` | — | **MoE** (A3B = 3B active of 30B), Q3_K_XL weights |

All three files exist. Also at `/mnt/ssd1/models/mistral-7b-instruct-v0.1.Q4_K_M.gguf` (the model the squatting :8088 is serving); both copies are equivalent.

## GPU state (Arc A770 on renderD128)

```
$ fuser /dev/dri/renderD128
/dev/dri/renderD128:  5814 90996 93227
$ ps -p 5814 -o comm=     ; echo codium-insiders
$ ps -p 90996 -o comm=    ; echo chrome (google-chrome-unstable)
$ ps -p 93227 -o comm=    ; echo qoder
```

No llama or SYCL process currently holds renderD128. The render node is busy with desktop compositor work (VSCodium, Chrome, Qoder) but **GPU compute is effectively idle** for inference purposes. Before any timing-sensitive run, re-check with `fuser /dev/dri/renderD128` — if a llama-server PID appears there, gate the run.

## Cross-references

- `RALPH_PROMPT.md` — loop mission, decisions log
- `RALPH_TASKS.md` — task queue (this is P0.0's deliverable)
- `RALPH_PROGRESS.md` — append-only iteration log (to be created by P0.0 onward)
- `docs/research/` — shared research output (sibling non-git dir)
- `AGENTS.md` (this repo) — fork policy, **never push to upstream**