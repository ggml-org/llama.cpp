## Goal

A fresh tessera AGENTS.md that (a) actually describes this project instead of inherited llama.cpp policy, and (b) folds in the conductor layer (capability graph + routing rules + run-book protocol) as one of its sections. The model becomes the dynamic-routing runtime across alphaevolve / tessera-analyst / findings-curator / code-reviewer, with `.zcode/program/run-book.json` as the cross-session state.

## Deliverables (3 files)

### 1. `AGENTS.md` — full rewrite (~180 lines)

Replaces the 231-line inherited llama.cpp file. Structure:

**1. Header + AI policy (keep the parts that matter)**
- Title: "Instructions for tessera" (not llama.cpp)
- One-line project ID: Tessera = llama.cpp fork + calibrated per-tensor quantization (T640), native spec-decoding (DFlash/DSpark), Apple-ANE prefill. Author: Julian Alejandro Torres Nieto / Tribunus.dev. Multi-licensed (LICENSE-TESSERA noncommercial; upstream MIT).
- Core AI policy distilled from the old file but reframed: "AI-generated code is allowed. What is not allowed is shipping code you do not understand. The human owns every line." No ban threats, no ggml-org references. Keep the genuinely useful conventions: ASCII not unicode (`-`, `->`, `x`, `...`), concise comments (carry the 4 GOOD/BAD examples), don't split lines mid-sentence, read surrounding code before editing, PAUSE and ask before large/new-pattern changes. Replace "commit/push/PR ban" with the harness-level rule: "Confirm before any outward-facing or hard-to-reverse action; never write PR descriptions, commit messages, or reviewer responses."

**2. Project map (new — the section the old file lacked)**
Tight table of the distinctive surfaces so any agent landing cold knows where things live:
- `tools/tile640/` + `tools/tessera/` — Python quantizer + per-tensor calibration pipeline (T640)
- `tools/quantize/tessera/` — C++ quant side, Metal kernel, L1/L1.5/L2/L3/L5/L6 layers, test suite
- `tools/ane-mtp/` — ANE Core ML prefill toolkit (gemma 4 / qwen 3 MTP)
- `common/` — `ane-mtp.h/.mm`, `speculative-calibration.*`, `tessera-args.h`, `tessera-debug/`
- `src/` — llama core + tessera KV-cache variants (`*-dsa`, `*-dsv4`, `*-iswa`)
- `ggml/src/ggml-et/` — tessera uberkernel backend
- `ggml/src/ggml-metal/ggml-metal-tile640-interleaved.metal` — Tile640 dequant kernel
- `TesseraStudio/` — native Swift macOS/iOS app front-end
- Build variants: `build/` (default), `build-ane/` (ANE), `build-g0/` (g1 Metal)
- Pointer to `docs/PROJECT-STATUS.md` for Production/WIP/Legacy status of each pillar

**3. Workflow (new — how work actually happens here)**
- The evolve workflow: `scratch/<feature>/agent-X` in-flight → `evolve-review/<feature>/agent-X` review → `merge:` onto `main`. Branch namespaces: `champions/`, `evolve-baseline/wN`, `tessera/*`. `agent@tessera.local` is a first-class author.
- The `.zcode/alphaevolve/<run>/` directory IS the coordination protocol (already documented in the alphaevolve meta-skill; reference it, don't duplicate).
- Skills: workspace skills live in `.zcode/skills/` (tessera-analyst); user skills in `~/.zcode/skills/` (xlsx, skill-refiner). Check skills/ before starting.

**4. Program Routing — the conductor section (new)**
This is the dynamic-routing layer. Contents:
- **Capability graph table** (the four confirmed): alphaevolve (meta-skill + Agent subagent), tessera-analyst (workspace skill), findings-curator (built-in Agent subagent), code-reviewer (built-in Agent subagent). For each: what it does, how to invoke (load skill vs `Agent(subagent_type=...)`), route-in triggers.
- **Run-book protocol**: "Before dispatching program-level work, read `.zcode/program/run-book.json` (use `scripts/run-book.py show`); after each step, update it (`scripts/run-book.py update ...`). The file IS the protocol."
- **Routing rules** (priority order):
  1. Explicit user request for a capability → route directly.
  2. A wave just froze (`.zcode/alphaevolve/<run>/integration/stack-state.json` changed) → offer tessera-analyst + findings-curator.
  3. Open high/medium findings exist in `.zcode/alphaevolve/findings.jsonl` → offer findings-curator.
  4. About to promote a champion to main → mandatory code-reviewer gate + user approval.
  5. Ambiguous → ask the user; never guess.
- **Routing loop**: state-which-capability-and-why in the response BEFORE dispatching; dispatch; update run-book; next turn reflects new state.
- **Hard rules**: never auto-promote without code-reviewer + user approval; never fabricate run-book state; state the route out loud.

**5. Useful resources (trimmed, tessera-scoped)**
- Project docs: `docs/PROJECT-STATUS.md`, `docs/architecture.md`, `docs/runtime-aware-pipeline.md`, `docs/findings-*.md`, `docs/audit-*.md`
- The 5 design studies (inference-engines-comparison, ane-backend-deep, vllm-concurrency, moe-disk-offload, interleaved-kernel-design)
- Build: `docs/build.md`; Server: `tools/server/README.md`
- Inherited llama.cpp resources (HOWTO-add-model, PEG/auto parser, Jinja) demoted to a short "inherited from upstream" sublist, clearly labeled as llama.cpp references
- Drop: ggml-org issue/PR links (not this project), the PR-template reference

### 2. `.zcode/program/run-book.json` — persistent state (~50 lines)

Same schema as the prior plan. Seeded from current repo state so it's immediately useful:
- `objective`: from the w1 ledger goal field
- `phases`: 5 completed memopt waves as `done` (capability=alphaevolve, artifact=path-to-ledger); one `pending` findings-curator phase (4 open high/medium findings); one `pending` tessera-analyst phase ("first research report not yet generated")
- `open_questions`: from the open findings summaries
- `decision_log`: empty initially (first decision recorded when conductor first runs)

Plain JSON, hand-editable, diff-friendly. Atomic writes from the helper script.

### 3. `scripts/run-book.py` — state helper (~80 lines, stdlib only)

Same as prior plan. CLI: `show` / `add` / `update <id>` / `decide` / `next`. Atomic write (temp + rename). Validates capability names against the four known ones. Creates with sensible default if missing. Matches the `scripts/alphaevolve-metrics.py` precedent of small stdlib CLIs.

## What gets dropped from the inherited AGENTS.md (stated explicitly)

- The "Instructions for llama.cpp" framing and all ggml-org references
- The "PR represents a long-term commitment to a small team of maintainers" framing (this is an owner-operated fork)
- The "fully autonomous agents operating without human oversight: do not contribute" ban threat
- The `ggml-gh-bot` exception note (not relevant here)
- The llama.cpp issue/PR search links and PR template reference
- The COMMENT-style GOOD/BAD examples are KEPT (4 of them) — they're genuinely good guidance anywhere, and the rewrite carries them verbatim into the conventions section
- The ASCII-not-unicode rule is KEPT
- The "PAUSE and ask before large changes" rule is KEPT

## What is NOT in scope

- No changes to the four capabilities themselves (alphaevolve skills, tessera-analyst skill, the built-in agent types) — they remain self-contained
- No build changes
- No new agent types (ZCode doesn't support them)
- No background scheduler — routing is reactive on user turns

## Files touched

```
AGENTS.md                                  (full rewrite, ~180 lines, replaces 231)
.zcode/program/run-book.json               (new, seeded from current repo state)
scripts/run-book.py                        (new, ~80 lines, stdlib only)
```

No edits to existing skills or scripts. No deletions (the old AGENTS.md content is in git history if ever needed).

## Verification (post-build)

1. `python3 scripts/run-book.py show` prints seeded state (5 done waves + pending curator + pending analyst).
2. `python3 scripts/run-book.py next` identifies the next pending phase correctly.
3. `python3 scripts/run-book.py add --capability tessera-analyst --summary "test"` → `show` reflects it; JSON still valid; file mtime changes (atomic write).
4. AGENTS.md parses as valid markdown; ~180 lines; the 4 comment-style examples and ASCII rule survived; all internal doc references resolve (`docs/PROJECT-STATUS.md`, `docs/architecture.md`, etc. — will verify each path exists before referencing).
5. The capability graph references exact invocation names: `alphaevolve` (subagent_type), `findings-curator` (subagent_type), `code-reviewer` (subagent_type), `tessera-analyst` (workspace skill).

Restart ZCode afterward so the new AGENTS.md routing section loads.