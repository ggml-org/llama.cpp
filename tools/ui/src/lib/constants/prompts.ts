/**
 * Special pseudo-categories used by the prompts library filter UI.
 * `ALL` matches every prompt; `OTHER` matches prompts without a category.
 */
export const PROMPT_CATEGORY = {
	ALL: 'all',
	OTHER: '__other__'
} as const;
