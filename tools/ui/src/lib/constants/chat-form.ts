export const INITIAL_FILE_SIZE = 0;
export const PROMPT_CONTENT_SEPARATOR = '\n\n';
export const CLIPBOARD_CONTENT_QUOTE_PREFIX = '"';
export const PROMPT_TRIGGER_PREFIX = '/';
/**
 * Trigger character that opens the file/folder mention picker when typed
 * at a token boundary in the chat textarea. Replaces the previous MCP
 * resources inline-picker behavior; MCP resources remain available via
 * the Add dropdown > MCP Resources browser dialog.
 */
export const MENTION_TRIGGER_PREFIX = '@';
export const NEW_CHAT_DRAFT_KEY = '__new_chat__';
