/* Tessera Observatory v1 -- selection controller (placeholder).
 *
 * Reserved for the future "selection is a constitutional event"
 * pattern. Prism Engine's selection-controller.js turns browser
 * text selection into a typed component event. Tessera's
 * bootstrap does not need this; the hook is here so the script
 * tag in <head> does not 404.
 */
(function () { 'use strict'; })();
