/* Tessera Observatory v1 -- transitions orchestrator (placeholder).
 *
 * Reserved for the future Astro-style view transitions wiring
 * (hero -> sub-page morph). The bootstrap ships without it; add
 * when the first "morph" target (e.g. a hero image that
 * persists across navigation) is introduced.
 *
 * The hook is here so the script tag in <head> does not 404.
 */
(function () { 'use strict'; })();
