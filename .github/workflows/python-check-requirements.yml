# DISABLED: Python check requirements.txt
# This workflow has been disabled to reduce CI coverage-like runs
# Original workflow provided requirements validation but added overhead
# To re-enable: uncomment the entire workflow below

name: Python-Check-Requirements-DISABLED

on:
  workflow_dispatch: # Only manual triggering allowed, no automatic runs

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Disabled
        run: echo "Python check requirements workflow has been disabled to reduce CI coverage-like runs"

# Original workflow (commented out):
# on:
#   push:
#     paths:
#       - '.github/workflows/python-check-requirements.yml'
#       - 'scripts/check-requirements.sh'
#       - 'convert*.py'
#       - '**/requirements*.txt'
#   pull_request:
#     paths:
#       - '.github/workflows/python-check-requirements.yml'
#       - 'scripts/check-requirements.sh'
#       - 'convert*.py'
#       - '**/requirements*.txt'
# 
# concurrency:
#   group: ${{ github.workflow }}-${{ github.head_ref && github.ref || github.run_id }}
#   cancel-in-progress: true
# 
# jobs:
#   python-check-requirements:
#     runs-on: ubuntu-latest
#     name: check-requirements
#     steps:
#       - name: Check out source repository
#         uses: actions/checkout@v4
#       - name: Set up Python environment
#         uses: actions/setup-python@v5
#         with:
#           python-version: "3.11"
#       - name: Run check-requirements.sh script
#         run:  bash scripts/check-requirements.sh
